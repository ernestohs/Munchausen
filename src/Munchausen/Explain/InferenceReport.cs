namespace Munchausen;

/// <summary>
/// A structured explanation of how a definition resolves every member. Its
/// members are added in a later milestone; it is declared here because
/// <see cref="LieDefinition{T}.Explain"/> returns it.
/// </summary>
public sealed class InferenceReport
{
    internal InferenceReport()
    {
    }
}
